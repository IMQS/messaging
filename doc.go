/*
Package messaging sends SMS notifications to large batches of numbers

Overview:

The messaging service exposes an HTTP API that allows sending of SMS messages to
stand owners or consumers when there is a planned or unplanned interruption to services.
It includes user authentication, SMS provider integration and records all activity in a SQL database.

*/

package messaging
